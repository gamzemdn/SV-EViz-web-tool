version: '3'

services:
  dashapp:
    build: .
    ports:
      - "8040:8040"
    volumes:
      - ./uploaded_files:/app/uploaded_files
    environment:
      - PYTHONUNBUFFERED=1
      - CHROME_PATH=/usr/bin/chromium
      - BROWSER_PATH=/usr/bin/chromium